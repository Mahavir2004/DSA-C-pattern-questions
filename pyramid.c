#include <stdio.h>

int main(){
int n,k;
printf("Enter a number: ");
scanf("%d", &n);
printf("Enter the 2nd number: ");
scanf("%d", &k);
for (int i = 1; i <= n; i++)
{
    for (int j = 1; j <= k ; j++)
    {
        
    }
    
}

     return 0;
}